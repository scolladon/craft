pipeline:
  insert:
    - id: bench
      archetype: harness
      contract:
        - harness-exec
      procedure: forge:bench
      role: forge:bench-runner
      after: validation
      consumes:
        - change
      produces:
        - bench-report
      gate: node --test engine/test/bench.test.js
