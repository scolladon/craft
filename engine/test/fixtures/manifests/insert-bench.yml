pipeline:
  insert:
    - id: bench
      archetype: harness
      contract:
        - harness-exec
      procedure: craft:bench
      role: craft:bench-runner
      after: validation
      consumes:
        - change
      produces:
        - bench-report
