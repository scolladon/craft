---
description: Parallel multi-dimension review with per-dimension convergence over the current change; also useful standalone on any branch.
argument-hint: [--profile <name>] [--skip <phase,...>]
---

# craft:review

Input: $ARGUMENTS

Load `skills/review/SKILL.md` from this repository and follow it verbatim as
the review procedure — its preamble and per-dimension convergence steps are
the single source; do not restate, summarize, or re-derive them here. Thread
$ARGUMENTS through exactly as that skill's own `$ARGUMENTS` input, including
any `--profile`, `--skip`, `--config`, `--harness`, or `--policy` flags it
may carry.
