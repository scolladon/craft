---
description: Scaffold a craft customization by interview and write a named manifest.
argument-hint: [manifest-name]
---

# craft:init

Input: $ARGUMENTS

Load `skills/init/SKILL.md` from this repository and follow it verbatim as
the manifest generator procedure — its preamble, interview, and lint-then-land
steps are the single source; do not restate, summarize, or re-derive them
here. Thread $ARGUMENTS through exactly as that skill's own `$ARGUMENTS`
input.
