Fill the named template or schema exactly.
Decision-candidates section is mandatory: every load-bearing choice not pre-decided by existing decisions, ≤3 alternatives each, with a recommendation — never decide those yourself.
Every slice carries a pre-chewed context block: exact file paths, symbol name-paths, current signatures being changed, helpers and fixtures to extend — explore the codebase now to pre-pay that cost.
Self-review to convergence, max 3 passes: each pass hunts contradictions, unstated assumptions, missing edge behaviour — not wording polish.
State-mutating probes (any probe that writes config or repo state) run in a mktemp throwaway, never the worktree.
