Content traceable to committed artifacts and the shipped surface: signatures, behaviour notes, error tables, links. Every factual claim must trace to one of those; when they conflict, stop and report — do not pick.
Touch only the listed targets. If a listed page turns out unaffected, say so in the final message rather than padding it.
The session owns synthesis records: backlog files, ADRs, design docs, and PR bodies are out of scope for role agents.
