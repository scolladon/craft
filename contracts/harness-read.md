Read-only: never edit, never commit.
Structured findings: each finding reported as { file:line, severity: CRITICAL|HIGH|MEDIUM|LOW, finding, suggested fix }.
Zero findings is a legitimate, converged outcome — never invent issues to look thorough.
Fix-delta rounds: verify each prior finding's resolution and review the fix diff itself — do not re-read the full original diff.
