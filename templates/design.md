# Design — <topic>

> Brief: <one-line restatement of the resolved input>
> Status: draft → self-reviewed ×N → accepted

## Context

<!-- What exists today: the subsystems this touches, the patterns the feature must
     follow, links to prior design docs / ADRs that constrain this one. -->

## Requirements

<!-- What must be true when this ships. Verifiable statements, not aspirations. -->

## Design

<!-- The substance: data shapes, flows, surfaces, error semantics, edge behaviour.
     When external behaviour must be matched (another tool, a spec, a wire format),
     PIN IT EMPIRICALLY and record the pinned matrix here — never design from memory
     of the external system. -->

## Decision candidates

<!-- REQUIRED section, may be empty ("none — fully pre-decided by existing ADRs").
     Every load-bearing choice not pre-decided by existing ADRs: ≤3 alternatives each,
     with a recommendation. The designer NEVER decides these; the user does, in the
     ADR phase. -->

| # | Choice | Alternatives (≤3) | Recommendation | Why |
|---|---|---|---|---|

## Test strategy

<!-- What proves it: unit/integration surfaces, fixtures, property-test lenses if a
     parser/matcher/round-trip pair is touched, edge matrix. -->

## Out of scope

<!-- Explicitly not in this change, with one line of why each. -->
