# <PR title — the conventional-commit summary of the change>

<!-- Reader-first PR body. Three narrative sections carry the story
     (Background → Intuition → Code); a trailer carries craft's traceability payload.
     Tone: clarity and flow over ceremony — engaging, classic style, smooth transitions
     between sections. A concrete toy example beats an abstract description. Use diagrams
     freely: reach for a mermaid block whenever a shape, a flow, or a before/after is
     easier seen than read. No quiz. -->

## Background

<!-- The existing system this change touches, in two skippable layers:
     - Deep: enough for a newcomer to follow — a reader who already knows it skips ahead.
     - Narrow: the specific state or behaviour this change depends on and departs from. -->

## Intuition

<!-- The core idea in one breath — the essence, not the mechanics. Open with a concrete
     toy example on small data. This is where a mermaid diagram or a before/after sketch
     earns its place most. Say why this shape, and not the obvious alternative. -->

## Code

<!-- A high-level walkthrough of the diff, grouped and ordered the way a reviewer should
     read it — by idea, not file-by-file. Point at the load-bearing hunks; let the rest
     ride. -->

---

### Provenance & verification

<!-- Craft's traceability payload, kept as a checklist rather than prose. Fill every line
     the run produced; where one is genuinely empty, write "none" instead of dropping it. -->

- **Decisions:** <ADR-### … or none>
- **Design:** <docs/design/<slug>.md or none>
- **Divergences:** <plan → implementation deltas, or none>
- **Pinned behaviours:** <invariants locked by tests, or none>
- **Test plan:** <what proves it — suites run, coverage, manual checks>
- **Run record:** <phase outcomes, POLICY and gate tokens from the run>
